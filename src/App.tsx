import React from 'react';

/**
 * Legacy Vite SPA entry (not used by Next.js App Router).
 * Keep as a no-op to satisfy builds that still scan this file.
 */
export default function App() {
  return null;
}
